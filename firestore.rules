rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissão total em ambiente de desenvolvimento / prototipagem
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

